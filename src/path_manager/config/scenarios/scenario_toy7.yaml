/**:
  ros__parameters:
    # SAM Defense Penetration scenario
    # Air defense network with overlapping SAM and AAA coverage

    # Threat Zone Format: [center_x, center_y, center_z, detection_range, engagement_range, max_threat_level]
    # - center (x,y,z): Position in meters
    # - detection_range: Radar detection radius (m)
    # - engagement_range: Weapon effective range (m)
    # - max_threat_level: Threat intensity (SAM≈100-200, AAA≈50-90)
    threat_zones: [
      # SAM Site 1 - blocking direct path
      36.0, 39.0, 2.0,      # center_x, center_y, center_z
      15.0,                 # detection_range (m)
      20.0,                 # engagement_range (m)
      100.0,               # max_threat_level (SAM)

      # SAM Site 2 - overlapping coverage
      83.0, 72.0, 7.0,     # center_x, center_y, center_z
      30.0,                # detection_range (m)
      35.0,                 # engagement_range (m)
      200.0,               # max_threat_level (SAM, high priority)

      # AAA Site 1 - medium threat
      73.0, 146.0, 2.0,     # center_x, center_y, center_z
      20.0,                # detection_range (m)
      25.0,                 # engagement_range (m)
      90.0,                # max_threat_level (AAA)
    ]
