/**:
  ros__parameters:
    # 실험 S1: 우회 가능 시나리오
    # 시작-목표 직선 경로 옆에 방공망 1개만 배치. 우회 모드 정상 동작을 검증.
    # 시작점과 목표점은 scenario_* yaml에서 별도 지정한다.
    threat_zones: [
      # SAM Site: 직선 경로에서 옆으로 치우친 위치
      25.0, 8.0, 0.0, 10.0, 5.0, 100.0
    ]
