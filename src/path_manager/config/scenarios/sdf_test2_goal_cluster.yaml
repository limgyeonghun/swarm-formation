/**:
  ros__parameters:
    # =============================================================
    # Scenario 2: Goal inside overlapping SAM cluster
    # Pairs with formation_manager/config/scenario_sdf_test1.yaml
    # start = (610.59, 108.07), goal = (155.60, 854.72)
    #
    # Goal sits at the center of a primary SAM. Four outer SAMs
    # surround it with overlapping rings so the drone must pick the
    # thinnest overlap to penetrate. Any approach axis is at least
    # partially covered.
    # =============================================================

    obstacles: [0.0, -9999.0, 0.0]

    # [cx, cy, cz, detection_range, max_threat_level]
    threat_zones: [
      # Primary SAM protecting the goal itself (goal is inside!)
      155.60, 854.72, 0.0, 100.0, 100.0,

    ]
