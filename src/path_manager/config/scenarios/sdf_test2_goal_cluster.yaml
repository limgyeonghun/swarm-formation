/**:
  ros__parameters:
    # =============================================================
    # Scenario 2: Goal inside overlapping restricted zone cluster
    # Pairs with formation_manager/config/scenario_sdf_test1.yaml
    # start = (610.59, 108.07), goal = (155.60, 854.72)

    # =============================================================

    obstacles: [0.0, -9999.0, 0.0]

    # [cx, cy, cz, detection_range, max_risk_level]
    risk_zones: [
      # Primary restricted zone protecting the goal itself (goal is inside!)
      155.60, 854.72, 0.0, 100.0, 100.0,

    ]
