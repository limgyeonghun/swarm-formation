/**:
  ros__parameters:
    # 실험 S2: 돌파 강제 시나리오
    # 시작-목표 직선 경로를 가로막는 방공망 배치. 우회 경로가 존재하지 않아
    # RRT*가 방공망을 돌파하게 되며, 이 상태에서 SFC가 어떻게 반응하는지 관측.
    threat_zones: [
      # SAM Site: 직선 경로 정중앙에 위치 + 넓은 detection으로 우회 차단
      25.0, 0.0, 0.0, 20.0, 10.0, 150.0
    ]
