/**:
  ros__parameters:
    # SAM Defense Penetration scenario

    threat_zones: [
      # SAM Site 1 - blocking direct path
      20.0, 0.0, 0.0,      # center_x, center_y, center_z
      10.0,                 # detection_range (m)
      9.0,                 # engagement_range (m)
      100.0,               # max_threat_level (SAM)

      # SAM Site 2 - overlapping coverage
      40.0, -10.0, 0.0,     # center_x, center_y, center_z
      15.0,                # detection_range (m)
      9.0,                 # engagement_range (m)
      100.0,               # max_threat_level (SAM, high priority)

      # AAA Site 1 - medium threat
      40.0, 15.0, 0.0,     # center_x, center_y, center_z
      15.0,                # detection_range (m)
      9.0,                 # engagement_range (m)
      100.0,                # max_threat_level (AAA)
    ]
