/**:
  ros__parameters:
    # Basic scenario - simple obstacles only, no threat zones
    # Good for testing basic obstacle avoidance without threat field

    # Obstacle Format: [x, y, z] in meters
    obstacles: [
      0.0,  -8.0, 0.0,    # Obstacle #1 - test object at (0, -8, 0)
    ]

    # No threat zones in basic scenario
    # threat_zones omitted - will use declare_parameter() default (empty vector)
