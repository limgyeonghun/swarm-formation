/**:
  ros__parameters:
    # Obstacle avoidance scenario with mixed shapes

    # Obstacle Format:
    # Basic: [x, y, z] - uses default inflation
    # Circle: [x, y, z, 0.0, radius]
    # Rectangle: [x, y, z, 1.0, width, height]
    obstacles: [
      8.0,  0.0, 0.0,                    # Basic obstacle with default inflation

      15.0, -5.0, 0.0, 0.0, 4.5,          # Circle: radius 4.5m

      25.0, 5.0, 0.0, 1.0, 2.0, 7.0,      # Rectangle: 2m x 7m

      35.0, -8.0, 0.0, 0.0, 5.0,          # Circle: radius 5.0m

      50.0, 2.0, 0.0, 1.0, 7.0, 4.0,      # Rectangle: 7m x 4m

      60.0, -1.0, 0.0, 0.0, 3.8,         # Circle: radius 3.8m
    ]
