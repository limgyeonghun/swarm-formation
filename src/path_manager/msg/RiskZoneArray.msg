# Replaces the entire active risk-zone set in path_manager and
# path_visualization. Published by ObstacleScenarioPanel (and any other
# mission authority) at runtime; consumed by both planning and
# visualization nodes so they stay in sync.
RiskZoneSpec[] zones
