/**:
  ros__parameters:
    # Threat zones for air defense penetration scenarios
    # Format: [center_x, center_y, center_z, detection_range, engagement_range, max_threat_level]

    threat_zones: [
      # SAM Site 1 (blocking direct path)
      25.0, 0.0, 0.0,      # center position (x, y, z)
      7.0,                 # detection range (m)
      8.0,                  # engagement/kill range (m)
      100.0,                # max threat level (SAM = 100)

      # SAM Site 2 (overlapping coverage)
      40.0, -5.0, 0.0,    # center position
      15.0,                 # detection range
      8.0,                  # engagement range
      200.0,                # max threat level

      # AAA Site 1 (medium threat)
      40.0, 10.0, 0.0,     # center position
      15.0,                 # detection range
      6.0,                  # engagement range
      90.0,                 # max threat level (AAA = 50)
    ]
